# Rhythm patterns

## Rhythms

Each rhythms can be described with:

- a `name`
- multiple `alias`
- a corresponding `wikidata` Q-identifier
- a list of notable `works` (in Markdown syntax)
- a list of `category`
- a list of `source`
- a textual description in Markdown syntax

The textual description may reference other rhythms with this syntax: `` `|x-xx|` ``.

##

Chronotonic diagrams have been introduced by Gustafson (1988).

Kjell Gustafson. The graphical representation of rhythm. In (PROPH) Progress Reports from Oxford Phonetics, volume 3, pages 6–26, University of Oxford, 1988

## See also

- <https://www.npmjs.com/package/@tonaljs/rhythm-pattern>
